apiVersion: v1
kind: Service
metadata:
  name: {{ include "aiusecases.fullname" . }}
  labels:
    {{- include "aiusecases.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aiusecases.selectorLabels" . | nindent 4 }}
