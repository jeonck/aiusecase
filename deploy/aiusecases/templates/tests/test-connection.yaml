apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aiusecases.fullname" . }}-test-connection"
  labels:
    {{- include "aiusecases.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aiusecases.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
